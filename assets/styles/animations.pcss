@define-mixin fadeIn_animation {
    @keyframes dht-fadeIn {
        0% {
            opacity: 0;
            visibility: hidden; /* Prevent mouse events until visible */
        }
        100% {
            opacity: 1;
            visibility: visible; /* Allow mouse events */
        }
    }
}