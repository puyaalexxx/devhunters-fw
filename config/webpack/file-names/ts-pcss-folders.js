////// typescript and pcss folders

// Modules Folder
const modulesFolder = "";

// Extensions folder
const extensionsFolder = "extensions/";

// Extensions folders
const sidebarsFolder = `${extensionsFolder}sidebars/`;
const optionsFolder = `${extensionsFolder}options/`;

// Options folders
const containersFolder = `${optionsFolder}containers/`;
const groupsFolder = `${optionsFolder}groups/`;
const togglesFolder = `${optionsFolder}toggles/`;
const fieldsFolder = `${optionsFolder}fields/`;

module.exports = {
    // Modules folder
    modulesFolder,

    // Extensions folder
    extensionsFolder,

    // Extensions folders
    sidebarsFolder,
    optionsFolder,

    // Options folders
    containersFolder,
    groupsFolder,
    togglesFolder,
    fieldsFolder,
};
