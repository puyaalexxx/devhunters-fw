import "./Divider.pcss";

// Define a functional component
const Divider = () => {
    return <div className="dht-divider"></div>;
};

// Export the component so it can be used elsewhere
export default Divider;
