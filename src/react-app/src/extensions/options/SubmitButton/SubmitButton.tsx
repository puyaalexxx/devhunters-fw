import "./SubmitButton.pcss";

// Define a functional component
const SubmitButton = () => {
    return (
        <div className="dht-field-wrapper-main">
            <button className="dht-button dht-btn-big dht-submit">Save Changes</button>
        </div>
    );
};

// Export the component so it can be used elsewhere
export default SubmitButton;
