import "./Tooltip.pcss";

// Define a functional component
const Tooltip = () => {
    return <div className="dht-info-help dashicons dashicons-info" data-tooltips="Tooltip description" data-position="OnLeft"></div>;
};

// Export the component so it can be used elsewhere
export default Tooltip;
